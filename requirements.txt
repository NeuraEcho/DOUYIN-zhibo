# ============================================
# AI 云端口播直播系统 - Python 依赖清单
# ============================================

# ---------- LangGraph / LangChain 编排层 ----------
langgraph>=0.4.0
langchain-core>=0.3.0
langchain-openai>=0.3.0

# ---------- HTTP 异步请求（云端 API 调用） ----------
httpx>=0.28.0

# ---------- Web 管理后台框架 ----------
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9

# ---------- 配置与序列化 ----------
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.1.0

# ---------- 日志与监控 ----------
loguru>=0.7.0

# ---------- 音频处理（CPU 轻量后处理） ----------
numpy>=1.26.0
sounddevice>=0.5.0

# ---------- SSE 流式解析 ----------
sse-starlette>=2.0.0

# ---------- 定时任务调度 ----------
apscheduler>=3.11.0

# ---------- WebSocket 客户端（弹幕监听） ----------
websockets>=14.0

# ---------- 测试 ----------
pytest>=8.0.0
pytest-asyncio>=0.25.0
